export const rootAuthPath = '/auth';
export const rootMainPath = '/main';

export const authPath = {
  signIn: `${rootAuthPath}/sign-in`,
};

export const mainPath = {
  dashboardProjects: { path: `${rootMainPath}/projects`, pathName: 'Projects' },
};
